# Modeling Units

- Work in millimeters (`mm`) for all dimensions unless the user explicitly requests another unit.
- Use increments of `5 mm` for dimensions, offsets, spacing, slider values, and other geometry controls unless the task requires finer precision.
- When converting from another unit, convert to millimeters and round to the nearest `5 mm` unless exact dimensions are required.
